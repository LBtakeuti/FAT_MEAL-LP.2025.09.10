# Future Tasks（将来対応タスク）

このファイルは「すぐには着手しないが、いずれ対応が必要なタスク」を記録するための場所。
FB管理体系（FB-XXX-NNN）が未整備のため、暫定的にFシリーズ番号で管理する。

| 番号 | タイトル | 優先度 | 期限目安 | 起票日 |
|------|---------|--------|---------|--------|
| F5 | 2027年以降の祝日リスト追加 | 中 | 2026年内 | 2026-05-25 |
| F14-3 | コラム記事 view_count の二重インクリメント懸念解消 | 低 | 未定 | 2026-05-28 |
| F14-4 | コラム機能の E2E テスト再実行（F14-2 Docker障害でブロック分） | 低 | 環境復旧後 | 2026-05-28 |
| F15-1 | 管理ダッシュボード: lucide-react 導入検討 | 低 | 未定 | 2026-05-28 |
| F15-2 | dashboard/summary API の Stripe invoices 呼び出し最適化 | 低 | 未定 | 2026-05-28 |
| F16-1 | 記事投稿 HTML↔Markdown 往復で失われる書式の運用評価 | 低 | 運用開始後 | 2026-05-28 |
| F16-2 | 記事編集画面のプレビュー機能（横並びpreview pane） | 中 | 未定 | 2026-05-28 |
| F16-3 | TipTapエディタ画像エラーの window.alert を toast に統一 | 低 | 未定 | 2026-05-28 |
| F16-4 | 大コンテンツの marked lazy import 化（パフォーマンス対策） | 低 | 問題顕在化時 | 2026-05-28 |
| F19-1 | subscription-delivery cron の未使用 shipping_address SELECT 整理 | 低 | 未定 | 2026-06-01 |
| F22-1 | サブスクプラン選択時の disabled 視覚フィードバック強化 | 低 | UX改善時 | 2026-06-01 |
| F32-1b | /admin/calendar 自由期間レンジ対応 | 中 | 未定 | 2026-06-08 |
| F37-1 | activate-subscription 通常フローの 400 正規化 | 低 | 未定 | 2026-06-09 |
| F39-A | reset-password の confirm 用 show/hide state を分離 | 低 | 未定 | 2026-06-09 |
| F39-B | forgot-password 列挙耐性強化（catch内でも setSent） | 低 | 未定 | 2026-06-09 |
| F39-1 | パスワードリセットメール文面の日本語カスタマイズ | 中 | 未定 | 2026-06-09 |
| F40-A | billingPortalError state を per-subscription 化 | 低 | 未定 | 2026-06-09 |
| F41-A | 住所変更モーダルの 〒重複表示確認 | 低 | 未定 | 2026-06-09 |
| F41-B | 住所変更後トーストの自動消去タイマー追加 | 低 | 未定 | 2026-06-09 |
| F43-A | auto-signup の既存メールエラーを error code 化 | 低 | 未定 | 2026-06-09 |
| F37-救済 | 3DS不整合バグで「カード登録のみ未契約」状態のユーザー手動救済 | 中 | 該当者発見時 | 2026-06-09 |
| F38-救済 | cron 重複バグで生成された orders の DB クリーンアップ | 中 | DB調査後 | 2026-06-09 |
| F29-1 | dashboard upcomingDeliveriesCount の除外メール対応 | 低 | 未定 | 2026-06-09 |
| F30-1 | dashboard 削除した日付ピッカー等を /admin/analytics へ集約 | 低 | 未定 | 2026-06-09 |
| F35-1 | listUsers ページネーション対応（1000件超対応） | 低 | 件数増加時 | 2026-06-09 |

---

## F19-1: subscription-delivery cron の未使用 shipping_address SELECT 整理

### 概要
F19 で全配送完了メール機能を削除した後、`app/api/cron/subscription-delivery/route.ts` 内の
`subscriptions.shipping_address` を SELECT している処理が dead code として残存している。

### 背景
- F18 で `sendSubscriptionCompletionEmail` 関数本体を削除
- F19 で `completeSubscription` 関数自体も削除
- 結果、shipping_address SELECT 処理が呼ばれない状態に
- 機能には影響なし、メンテナンス時の混乱要因

### 対象ファイル
- `app/api/cron/subscription-delivery/route.ts`

### 対応方針（暫定）
- shipping_address SELECT 処理を削除
- 関連する `void subscription;` の lint 回避コードも削除

### 優先度
- 低（現状動作には一切影響なし）

### 起点
- F18/F19 実装時の譲歩（2026-06-01）
- 関連コミット: 61d467b (F18) / bd3202c (F19)

---

## F22-1: サブスクプラン選択時の disabled 視覚フィードバック強化

### 概要
F22 でサブスクプラン選択時に同意ゲートを実装したが、「ボタンを押しても進まない」と感じる
ユーザーがいる可能性。視覚フィードバック（プラン選択ボタンの disabled スタイル等）を強化する。

### 背景
- F22 実装後、サブスクプラン選択は「選ぶ → 同意チェック → 再クリック」の2クリック必要
- ゲート機能は確実だが、初見ユーザーには分かりにくい可能性
- A/Bテスト等でコンバージョン低下が見られたら対応推奨

### 対象ファイル
- `components/purchase/PurchaseFlow.tsx`
- `components/purchase/PlanSelectorCards.tsx`（または相当ファイル）

### 対応方針（暫定）
- PlanSelectorCards に `disabled` prop を追加
- 同意未チェック時にプラン選択ボタンに視覚的なヒント表示
- またはチェックボックスをより目立たせる配置に

### 優先度
- 低（機能上は問題なく動作）

### 起点
- F22 実装時のレビュー指摘（2026-06-01）
- 関連コミット: 16bc2ca

---

## F16-1: 記事投稿 HTML↔Markdown 往復で失われる書式の運用評価

### 概要
F16 で TipTap (HTML出力) → turndown → Markdown 保存 → marked → HTML(編集ロード時) の往復変換を採用した。
シンプルブログでは問題ないが、複雑な書式（カスタム属性、特殊なネスト等）で書式が失われる可能性がある。

### 背景
- F16 実装時、公開側 ArticleContent.tsx の react-markdown 互換を維持するため、A案（往復変換）を採用
- リード判断で「シンプルブログでは実害なし」として進めたが、運用開始後に評価が必要

### 対応方針（暫定）
- 運用1ヶ月程度の記事を確認し、書式損失パターンを特定
- 問題があれば turndown のカスタムルール追加で対応

### 優先度
- 低（現状実害なし）

### 起点
- F16 実装時の譲歩（2026-05-28）
- 関連コミット: 762ac6e

---

## F16-2: 記事編集画面のプレビュー機能（横並びpreview pane）

### 概要
記事編集画面で、編集中の内容を /blog/[slug] と同じ見た目でリアルタイムプレビューできる機能を追加する。
F16 ではスコープ外として「公開ページを開く」リンクで代替（公開済み記事のみ）。

### 対応方針（暫定）
- 編集画面に2カラムレイアウト（左: エディタ / 右: プレビュー）
- プレビュー側は ArticleContent.tsx と同じレンダリングを使用
- リアルタイム更新（debounce 300ms程度）

### 優先度
- 中（記事入稿のしやすさに直結）

### 起点
- F16 実装時のスコープ外（2026-05-28）
- 関連コミット: 762ac6e

---

## F16-3: TipTapエディタ画像エラーの window.alert を toast に統一

### 概要
F16 の ArticleRichTextEditor で画像アップロード失敗時 `window.alert` を使用している。
他の管理画面で使われている useToast に統一する。

### 対象ファイル
- `components/admin/articles/ArticleRichTextEditor.tsx`

### 優先度
- 低（機能には影響なし、UX統一のみ）

### 起点
- F16 レビュー指摘（2026-05-28）
- 関連コミット: 762ac6e

---

## F16-4: 大コンテンツの marked lazy import 化（パフォーマンス対策）

### 概要
記事編集画面のロード時、marked を同期で実行している。大コンテンツの記事で体感遅延が発生する可能性。

### 対応方針（暫定）
- marked を dynamic import に変更
- ロード中はスケルトン表示

### 優先度
- 低（問題が顕在化したら対応）

### 起点
- F16 実装時の指摘（2026-05-28）
- 関連コミット: 762ac6e

---

## F15-1: 管理ダッシュボード - lucide-react 導入検討

### 概要
F15 実装時、アイコンライブラリ `lucide-react` を使う指示があったが、`package.json` に未導入だったため
既存パターンを踏襲してインラインSVG（`components/admin/dashboard/icons.tsx`）で実装した。
将来 lucide-react を導入する方針があれば、このダッシュボード含む全アイコンを統一できる。

### 背景
- F15 デザインチェックで「既存踏襲として正しい」と承認されたが、ベストではない
- 管理画面の他ページでも基本的にインラインSVGまたはテキストアイコンを使用している
- lucide-react を入れる場合は全体方針の決定が必要

### 対象ファイル
- `components/admin/dashboard/icons.tsx`
- `app/admin/layout.tsx`（既存インラインSVG部分）
- 他、インラインSVGで実装されている管理画面全般

### 対応方針（暫定）
- `pnpm add lucide-react`
- インラインSVGを lucide-react コンポーネントに順次差し替え
- ただし全体方針として「インラインSVGで統一」を選ぶ判断もあり得る

### 優先度
- 低（現状機能には問題なし）

### 起点
- F15 実装時の譲歩（2026-05-28）
- 関連コミット: c97345a（F15）

---

## F15-2: dashboard/summary API の Stripe invoices 呼び出し最適化

### 概要
`/api/admin/dashboard/summary` で、Stripe invoices.list の `for await` ループが
①今月売上 と ③累計売上 の2箇所で **独立に実行されている**。
レスポンス速度向上のため、将来1ループに統合できる余地がある。

### 背景
- F15 レビュー指摘事項（提案レベル）
- 正確性には問題なし。レスポンス速度のみの最適化
- Stripe API のレートリミット観点でも改善余地あり

### 対象ファイル
- `app/api/admin/dashboard/summary/route.ts`

### 対応方針（暫定）
- Stripe invoices を1度だけ全期間で取得
- メモリ上で「今月分」と「累計」をそれぞれ集計
- ただし全期間取得のメモリ負荷とトレードオフ。データ量増加時の挙動を要評価

### 優先度
- 低（現状実害なし）
- ダッシュボードのレスポンスが体感で遅くなった時に着手

### 起点
- F15 実装時のレビュー指摘（2026-05-28）
- 関連コミット: c97345a（F15）

---

## F14-3: コラム記事 view_count の二重インクリメント懸念解消

### 概要
コラム記事詳細の閲覧数（`articles.view_count`）が、SSRと内部APIの両方でインクリメントされる実装になっている。
現状はSSRしか稼働していないため二重カウントは起きていないが、将来クライアントからAPIを呼ぶ実装に変えた場合に重複する。

### 背景
F14-2（コラムUIリニューアル）実装時のレビュー指摘事項。
- `app/blog/[slug]/page.tsx`（Server Component）で view_count を +1
- `app/api/blog/[slug]/route.ts`（API Route）でも view_count を +1
両方が同時に走るシナリオが現状はないが、将来の改修時に事故が起きる可能性がある。

### 対象ファイル
- `app/blog/[slug]/page.tsx`
- `app/api/blog/[slug]/route.ts`

### 対応方針（暫定）
- view_count のインクリメント責任を片方に寄せる
- 推奨: API ルート側のインクリメントを削除（SSR集約）
- もしくは共通ヘルパー（lib/blog/incrementViewCount.ts 等）に切り出し、重複呼び出しをガード

### 優先度
- 低（現状実害なし）
- ただしクライアント側で API を呼ぶ改修を入れる前に必ず先に対応する

### 起点
- F14-2 実装時のレビュー指摘（2026-05-28）
- 関連コミット: ef6a0fd（F14-2）

---

## F14-4: コラム機能の E2E テスト再実行（F14-2 Docker障害でブロック分）

### 概要
F14-2 完了時、E2Eテストが Docker コンテナのコンパイル30分以上スタックにより5件失敗した。
実装コードに問題はないが、E2E観点での動作検証が未完了。

### 背景
- F14-2 のE2Eテスト実行時、futorumeshi-lp-dev コンテナが `Compiling /` で停止
- `docker compose restart` でコンテナを再起動したが、テスト再実行は未着手
- 実装本体は lint / 型 / build / ユニットテスト すべてグリーンのためリリース判断に問題なし

### 対象シナリオ
- /blog 一覧ページのスモークテスト（既存）
- /blog 一覧から /blog/[slug] への遷移テスト（追加予定）
- /blog/[slug] の Pick Up リンクの遷移テスト（追加予定）
- BlogSection の Splide カルーセル存在確認（追加予定）

### 対応方針
- Docker環境が安定した段階で e2etest-mate に再実行依頼
- 既存シナリオで網羅できない部分は新規 UAT シナリオとして追加

### 優先度
- 低（実装は他チェックでグリーン）

### 起点
- F14-2 実装時のE2E実行ブロック（2026-05-28）
- 関連コミット: ef6a0fd / f1f23ad / a579352

---

## F5: 2027年以降の祝日リスト追加

### 概要
`lib/business-days.ts` に登録されている祝日定数を、2027年以降にも拡張する。

### 背景
F4-2（配送カレンダーの配置基準を「配送作業日」に変更）の実装時、
土日祝をスキップするロジックを導入した。祝日判定は `lib/business-days.ts` 内の
定数リストに依存しているが、現状の登録は **2024〜2026 年まで**。

2027年到来後にこのファイルを更新しないと、本来「祝日」として扱うべき日が
営業日として扱われ、配送カレンダーの自動配置が業務実態と乖離するリスクがある。

### 対象ファイル
- `lib/business-days.ts`

### 対応方針（暫定）
- 内閣府のHPまたは祝日マスタAPIから2027年以降の祝日リストを取得
- 直接ハードコードでもよいし、年度ごとに自動生成する仕組みでもよい
- 既存リストの形式に合わせて追記

### 優先度
- 中（2026年内に対応推奨）
- 2026/12/31 を過ぎても未対応の場合は優先度「高」に引き上げ

### 起点
- F4-2 実装時の指摘（2026-05-25）
- 関連コミット: 09494d6（F4-2）
